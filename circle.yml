deployment:
  production:
    branch: master
    commands:
      - AWS_S3_BUCKET=gocardless.com node_modules/.bin/gulp deploy

  staging:
    branch: dev
    commands:
      - AWS_S3_BUCKET=staging.gocardless.com node_modules/.bin/gulp deploy

machine:
  node:
    version: 0.10.22
